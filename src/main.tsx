import React from 'react'
import ReactDOM from 'react-dom/client'
import FaceApp from './app/FaceApp';


ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FaceApp />
  </React.StrictMode>,
)
